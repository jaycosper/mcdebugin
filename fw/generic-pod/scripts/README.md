# _PROJECT_/scripts
Supporting scripts for the project
- toolchain/flow defining CMake files
- prechecks (Lint, Format, rules, etc)

This may be shared across projects (project-agnostic), so could be moved to a common area later.